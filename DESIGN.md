# FlashLAN Design Contract

> 状态：项目级目标规范
> 适用范围：`src/layouts`、`src/views`、`src/ui`、`src/styles` 以及所有用户可见界面
> 相关文档：[页面与 Pattern 合同](docs/design/page-contracts.md) · [视觉验收清单](docs/design/visual-checklist.md)

本文档是 FlashLAN 的 UI 设计合同。它用于约束设计决策和实现方式，不是灵感集合，也不是某一张设计稿的文字说明。

文档中的关键词含义如下：

- **MUST / MUST NOT**：必须遵守；违反时不能视为完成。
- **SHOULD / SHOULD NOT**：默认遵守；偏离时需要明确说明理由。
- **MAY**：在不违反其他规则时可以采用。

现有界面中不符合本文档的部分可以分阶段迁移，但任何新增 UI 或被修改的区域 **MUST NOT** 引入新的违规样式。修改现有区域时，**SHOULD** 顺手迁移直接相关的局部 Pattern。

## 1. 产品设计立场

FlashLAN 是一个跨平台局域网工具，不是 SaaS Dashboard。

界面 **MUST** 表现为：

- 原生：尊重桌面端和移动端各自的交互习惯。
- 紧凑：一屏完成主要任务，不用大面积留白制造“高级感”。
- 安静：品牌色和装饰元素保持克制。
- 可靠：连接、信任、传输和错误状态始终清晰。
- 快速：最常用操作可以直接完成，不被二级容器和无关信息阻挡。

设计决策的优先级为：

1. 任务是否清楚。
2. 当前状态是否可见。
3. 操作是否直接。
4. 信息层级是否稳定。
5. 视觉是否精致。

当视觉表现与任务效率冲突时，**MUST** 优先保证任务效率。

## 2. 核心原则

### FL-PRINCIPLE-001 — 列表优先

设备、传输记录、会话和设置项 **MUST** 优先使用紧凑列表或分组行。

只有当内容是独立对象、拥有自己的状态和操作，并且确实需要与相邻对象分离时，才 **MAY** 使用 Card。

### FL-PRINCIPLE-002 — 单一主操作

同一个工作区域在同一状态下 **MUST NOT** 同时出现多个同等强调的 Primary Action。

流程可以在状态变化后转移主操作，例如：

```text
未选择文件：选择文件是主操作
已选择文件：发送文件是主操作
传输中：查看进度或取消成为主要反馈
```

### FL-PRINCIPLE-003 — 状态优先于装饰

品牌色 **MUST** 用于主操作、选中、进度和关键焦点；**MUST NOT** 用于大面积装饰背景。

成功、警告、错误、离线和信任状态 **MUST** 使用语义色，并同时提供文字或图标，不能只靠颜色表达。

### FL-PRINCIPLE-004 — 密度一致

页面可以有不同结构，但相同层级的标题、工具栏、列表行、按钮和辅助信息 **MUST** 保持一致的尺寸与间距。

不得为了填满空间而拉高 Card、空状态、上传区或列表项。

## 3. Design Tokens

### 3.1 颜色

项目沿用 `zinc` 中性色和 `indigo` 品牌色。实现时 **MUST** 使用主题的语义 Token：

- `background`：应用和页面背景。
- `foreground`：主要文字。
- `card`：确有必要的独立表面。
- `muted` / `muted-foreground`：次级表面和辅助文字。
- `border` / `input`：边框和输入控件边界。
- `primary`：主操作、选中、焦点和进度。
- `success` / `warning` / `destructive` / `info`：反馈状态。

页面代码 **MUST NOT** 直接使用 `violet-*`、`indigo-*`、`zinc-*` 等具体调色板类表达产品语义，也 **MUST NOT** 写入独立的 Hex、RGB、HSL 或 OKLCH 值。新增颜色应先进入主题层。

品牌色使用规则：

- 主导航选中项：`primary/8` 到 `primary/10` 背景，`primary` 文字或图标。
- Primary Button：允许实色背景。
- Secondary、Ghost 和普通列表行：不得使用实色品牌背景。
- 大面积页面、Card、空状态：不得使用品牌色填充。
- 同一工作区域 **MUST NOT** 出现两个以上实色 Primary Button。

### 3.2 间距

允许的基础间距为：

```text
4 / 8 / 12 / 16 / 24 / 32 px
```

语义映射：

| Token     | 数值 | 用途                     |
| --------- | ---: | ------------------------ |
| `space-1` |  4px | 图标内部、小型状态点     |
| `space-2` |  8px | 紧凑控件和同行元素       |
| `space-3` | 12px | 列表行内部               |
| `space-4` | 16px | 移动端页面边距、小型分组 |
| `space-6` | 24px | 桌面页面边距、主要分区   |
| `space-8` | 32px | 页面级大分区的最大间距   |

页面代码 **MUST NOT** 发明 `10px`、`18px`、`20px`、`28px` 等非规范间距。必要的系统安全区、进度宽度和布局 `calc()` 除外。

### 3.3 圆角

| Token         |  数值 | 用途                              |
| ------------- | ----: | --------------------------------- |
| `radius-sm`   |   6px | Badge、小型控件                   |
| `radius-md`   |   8px | Button、Input、导航项、列表选中项 |
| `radius-lg`   |  12px | Dialog、Popover、独立对象容器     |
| `radius-pill` | 999px | Avatar、状态点、真正的 pill       |

- 常规界面 **MUST NOT** 使用大于 `12px` 的圆角。
- `rounded-2xl`、`rounded-3xl` 和任意值圆角 **MUST NOT** 出现在页面实现中。
- 圆形 Avatar、状态点和进度条端点不受上述限制。

### 3.4 字体

使用系统 UI 字体栈。IP、端口、指纹、文件路径和文件大小等机器信息 **MAY** 使用等宽字体。

| 层级             | 字号 / 行高 |    字重 |
| ---------------- | ----------- | ------: |
| 页面标题         | 24 / 32px   | 650–700 |
| Section 标题     | 16 / 24px   |     600 |
| 组件标题         | 14 / 20px   |     600 |
| 正文             | 14 / 22px   |     400 |
| 辅助信息         | 12 / 18px   |     400 |
| Badge / 紧凑标签 | 12 / 16px   |     500 |

- 用户可见文字 **MUST NOT** 小于 `12px`。
- 页面代码 **MUST NOT** 使用 `text-[11px]` 等任意字号。
- 页面标题 **MUST NOT** 使用超大 Hero Typography。
- 相同层级 **MUST** 使用相同字号和字重，不得根据页面“凭感觉”调整。

### 3.5 图标

- 功能图标 **MUST** 使用 Lucide，并通过现有 `SIcon` 渲染。
- 常规图标为 `16px`，紧凑辅助图标为 `14px`，页面或空状态主图标最大为 `32px`。
- 图标容器通常为 `32px`；不得为了装饰创建 `48–64px` 的彩色圆角图标块。
- 功能界面 **MUST NOT** 使用 Emoji 代替图标。
- 仅图标按钮 **MUST** 提供可访问名称或 `aria-label`。

### 3.6 边框与阴影

- 普通容器和列表分组使用 `1px border` 或 divider 建立层级。
- Card 默认 **MUST NOT** 带阴影。
- Dialog、Popover、Dropdown、Toast 和悬浮传输面板 **MAY** 使用阴影。
- 页面内普通对象 **MUST NOT** 使用 `shadow-md`、`shadow-lg` 或更强阴影。
- 不得同时使用强边框、强背景和阴影表达同一层级。

### 3.7 动效

- Hover、Pressed、Selected 等微交互使用 `120–180ms`。
- Dialog 和 Drawer 进入/退出最长 `240ms`。
- 动效 **MUST** 表达状态变化，**MUST NOT** 只作为装饰。
- **MUST** 尊重 `prefers-reduced-motion`。
- 加载、传输和刷新状态不得通过布局跳动表达。

项目级 Motion Token：

| Token                  | 时长    | 用途                            |
| ---------------------- | ------- | ------------------------------- |
| `--fl-motion-fast`     | `150ms` | Hover、列表增删、反馈和页面切换 |
| `--fl-motion-standard` | `180ms` | Drawer、Dialog 和悬浮面板的显隐 |

- 页面切换只使用轻微透明度和 `4px` 位移，不使用缩放或大幅滑动。
- 列表增删、文件选择和异步反馈 **SHOULD** 使用统一状态过渡。
- 持续动画只用于真实加载、扫描或传输；任务结束后 **MUST** 停止。

## 4. Layout Contract

### 4.1 桌面端

桌面布局从 `768px` 开始启用。

| 区域                | 约束          |
| ------------------- | ------------- |
| Sidebar             | 固定 `224px`  |
| Sidebar 品牌区      | 高 `56px`     |
| 页面内边距          | `24px`        |
| 传文件内容宽度      | 最大 `1024px` |
| 设备 / 记录内容宽度 | 最大 `960px`  |
| 设置内容宽度        | 最大 `720px`  |
| 消息设备栏          | `224px`       |
| 消息正文列          | 最大 `720px`  |

页面内容 **MUST** 在可用区域内水平居中。消息页等工作区型页面可以占满主区域，但其文字和消息内容列仍 **MUST** 限宽。

### 4.2 移动端

- 移动端页面内边距为 `16px`。
- 主要触控目标 **MUST** 至少为 `44 × 44px`；视觉控件可以更小，但点击区域必须满足要求。
- 顶部栏和底部导航 **MUST** 处理 Safe Area。
- 双栏结构 **MUST** 转换为分步浏览，不能简单压缩成两列。
- 列表和设置项保持全宽；不得横向滚动承载核心信息。
- 桌面 Hover 操作在移动端 **MUST** 有始终可见或菜单内的等价入口。

### 4.3 页面标题区

所有常规页面 **SHOULD** 使用统一的 `PageHeader` Pattern：

```text
标题                                      页面级操作
一句话说明                               次级操作
可选的状态摘要
```

- 标题区不得包装在 Card 内。
- 页面级操作保持在右侧或移动端标题下方。
- 移动端最多两个仅图标操作时，**MAY** 与标题同行；每个操作的触控区域仍须至少 `44 × 44px`。
- 状态摘要使用辅助文字、状态点或紧凑 Badge，不得创建统计 Card。

## 5. Component Contract

### 5.1 Button

- 桌面默认高度 `32px`；移动端视觉高度建议 `40px`，触控区域至少 `44px`。
- Primary Button **MUST** 对应当前最重要、最可能执行的操作。
- Secondary 操作使用 Outline、Pure 或 Ghost。
- Destructive 操作默认使用 Ghost、菜单项或二次确认；不得与 Primary Action 争夺视觉权重。
- Disabled 和 Loading 状态 **MUST** 保持原尺寸，避免布局变化。
- 按钮文案 **SHOULD** 使用“动作 + 对象”，例如“发送文件”“添加设备”。

### 5.2 Input

- 桌面默认高度 `32px`。
- 输入框 **MUST** 有可见 Label、关联说明或足够明确的上下文，不得只依赖 Placeholder。
- 校验错误显示在字段附近，并说明如何修正。
- IP、端口和路径输入保持等宽信息展示，但 Label 和错误文案使用系统字体。

### 5.3 Card

Card 仅在内容同时满足以下条件时使用：

1. 表示独立对象或交互单元。
2. 拥有自己的状态或操作。
3. 与相邻内容分离能明显提升理解。

以下场景 **MUST NOT** 使用 Card：

- 仅为了包住标题和一段内容。
- 页面 Section 的默认分组。
- 每一条设备、记录、会话或设置项。
- 空状态的默认外框。

优先使用间距、Divider、浅层 Surface 和 Typography 建立层级。

### 5.4 List Row

| Pattern    | 高度    |
| ---------- | ------- |
| 设备行     | 72–80px |
| 传输记录行 | 64–72px |
| 设置行     | 56–64px |
| 会话行     | 56–64px |

- 列表使用 Divider 或统一浅色 Surface，不使用 Card 堆叠和大间距。
- 行内主要内容只有一个视觉焦点。
- 次要信息最多两行；超出时截断，并提供可访问的完整值。
- 行操作在桌面端可通过 Hover 显示，但移动端必须可发现。

### 5.5 Badge 与状态

- Badge 用于短状态或类别，不作为按钮替代品。
- 文案通常不超过 4 个汉字或一个短单词。
- 在线、完成、失败、离线、可信和未信任 **MUST** 使用稳定的语义映射。
- 同一状态不得同时出现多个含义重复的 Badge。
- 状态 **MUST** 包含文字、图标或其他非颜色信息。

### 5.6 Empty State

- 空状态区域桌面端最大高度 `240px`，移动端最大高度 `200px`。
- 图标最大 `32px`，允许一个标题、一句说明和最多一个操作。
- 空状态 **MUST NOT** 使用巨大彩色图标、插画或大面积 Card。
- 空状态文案说明“为什么为空”以及“下一步做什么”。

### 5.7 Dialog、Popover 与浮层

- Dialog 最大圆角 `12px`，使用 Overlay 和允许的浮层阴影。
- 危险操作确认 **MUST** 明确说明影响范围。
- Popover 和 Dropdown 使用紧凑列表，单项触控区域在移动端至少 `44px`。
- 浮层关闭后焦点 **MUST** 返回触发元素。

### 5.8 Skeleton

- 骨架屏只用于真实的初始加载或刷新过程，任务结束后 **MUST** 立即停止动画。
- 骨架结构 **MUST** 近似最终内容的高度、密度和层级，避免加载完成后发生明显布局跳动。
- 骨架使用 `muted` Surface，不使用品牌色、阴影或装饰性渐变。
- 已知内容不应被无意义地长期隐藏；短暂刷新可以用骨架替换正在重新获取的区域。
- 加载容器 **MUST** 提供 `aria-busy`，并通过屏幕阅读器文案说明正在加载的对象。

## 6. Interaction States

每个可交互 Pattern **MUST** 定义适用的状态：

```text
default
hover（桌面）
pressed
focus-visible
selected
disabled
loading
error
```

异步操作还 **MUST** 满足：

- 开始后立即提供反馈。
- 重复提交被禁止或去重。
- 进度可用时显示真实进度，不使用伪进度。
- 错误信息说明对象、原因和恢复操作。
- 成功反馈不得阻挡下一步任务。

## 7. 内容与文案

- 产品界面默认使用简体中文。
- 同一对象名称必须稳定：使用“设备”“传输记录”“发送文件”“接收文件”，不得在页面间切换近义词。
- 标题描述任务，辅助文案解释条件或结果。
- 文案 **SHOULD** 简短直接，避免营销式语言。
- IP、端口、设备指纹、路径和版本号 **MUST** 保持原始可复制值。
- 长设备名、文件名和路径 **MUST** 正确截断；不得挤压状态和主要操作。

## 8. Accessibility

- 文字和图标对比度 **MUST** 满足 WCAG AA。
- 所有键盘可操作元素 **MUST** 有一致的 `focus-visible` 样式。
- 功能不能只通过 Hover、颜色、位置或动画表达。
- 图标按钮、状态点和进度 **MUST** 有可访问名称。
- Dialog **MUST** 管理焦点；Escape 和关闭按钮行为必须一致。
- 页面缩放到 `200%` 时，核心操作仍需可用。
- Dark Mode 不是简单反色；边框、Surface 和语义色必须通过主题 Token 生成。

## 9. 实现约束

### FL-CODE-001 — 使用项目 UI 层

页面 **MUST** 优先复用 `src/ui/components` 和已定义 Pattern。不得在页面中重新实现 Button、Input、Badge、Dialog、Switch 等基础控件。

### FL-CODE-002 — 禁止任意视觉值

`src/views` 和 `src/layouts` 中 **MUST NOT** 新增以下写法：

```text
rounded-[...]
text-[...]
shadow-[...]
p-[...]
m-[...]
gap-[...]
直接颜色值
```

允许的例外：系统 Safe Area、基于视口的必要 `calc()`、真实传输进度宽度，以及无法用语义 Token 表达的技术布局值。例外 **SHOULD** 带注释说明原因。

### FL-CODE-003 — Pattern 优先

一个视觉结构出现两次时 **SHOULD** 提取为 Pattern；出现三次时 **MUST** 提取。

优先建立的 Pattern 见 [页面与 Pattern 合同](docs/design/page-contracts.md)。

### FL-CODE-004 — 业务与视觉分离

纯设计任务 **MUST NOT** 顺带修改发现、配对、传输、历史或消息业务语义。UI Pattern 通过清晰 Props 和 Events 消费现有状态。

### FL-CODE-005 — 不直接修改生成层

若 SoybeanUI 组件源码需要调整，优先修改 `src/ui/styles` 和项目级封装。不得为了单页效果复制一份基础组件。

## 10. 明确禁止的 AI UI 模式

- Dashboard 式统计 Card 填充页面。
- Card 套 Card。
- 每个 Section 一个圆角白盒。
- 巨型虚线上传区占据大半屏幕。
- 超大标题和不必要的副标题。
- 紫色渐变、毛玻璃和装饰性光晕。
- 多个彩色 Badge 重复表达同一状态。
- 每个图标都放在大号彩色圆角方块中。
- 为了对称而添加无功能按钮。
- 大量 `rounded-full` 让普通按钮和列表变成胶囊。
- 用占位 Card 解决信息不足。
- 桌面布局直接压缩成移动端布局。

## 11. UI 变更流程

任何影响视觉或交互的修改 **MUST** 按以下顺序执行：

1. 阅读本文档和相关页面合同。
2. 明确复用或新增哪个 Pattern。
3. 先处理 Token / Pattern，再修改页面组合。
4. 覆盖正常、空、加载、错误和不可用状态。
5. 按 [视觉验收清单](docs/design/visual-checklist.md) 检查桌面端、移动端和 Dark Mode。
6. 运行项目的类型检查、Lint 和格式检查。

若用户提供的明确设计要求与本文档冲突，应优先遵循用户要求，同时更新本文档或记录局部例外，避免形成隐藏规则。

## 12. Definition of Done

UI 修改只有在以下条件全部满足时才算完成：

- 没有新增任意视觉值和直接调色板颜色。
- 使用了正确的主题 Token 和 Pattern。
- 没有新增不必要的 Card、阴影或大圆角。
- 主操作、状态和错误恢复路径清晰。
- 桌面端、移动端、亮色和暗色均已检查。
- 长文本、空状态、加载状态和错误状态可用。
- 键盘焦点、触控目标和可访问名称正确。
- `pnpm check` 通过，或明确说明与本次修改无关的既有失败。
